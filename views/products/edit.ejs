<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= product.name %>
  </title>
</head>

<body>
  <h1>Editing: <%= `${product.name} - $${product.price.toFixed(2)} - ${product.category}` %>
  </h1>
  <form action="/products/<%= product.id %>/edit?_method=PUT" method="POST">
    <label for="name">Product Name:</label>
    <input type="text" name="name" id="name" placeholder="Enter new product name..." value="<%=product.name%>">
    <label for="price">Product Price: $</label>
    <input type="number" step="0.01" name="price" id="price" placeholder="Enter new product price..."
      value="<%= product.price %>">
    <label for="category">Select New Product Category:</label>
    <select name="category" id="category">
      <% for( category of categories) { %>
        <option value="<%= category %>" <%=product.category===category ? "selected" : "" %>>
          <%= category %>
        </option>
        <% } %>
    </select>
    <button>Update product</button>
  </form>
  <a href="/products/stock#<%= product.name %>">Cancel</a>
</body>

</html>